<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:g="clr-namespace:gigIt.Xamarin"
             xmlns:ctrls="clr-namespace:gigIt.Xamarin.Forms.Controls"
             x:Class="gigIt.Xamarin.Forms.Views.AspectHeaderView">
    <ContentView.Content>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <BoxView BackgroundColor="{Static g:gigItColors.headerBG}" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="3"/>
            <ctrls:gigItIconButton 
                FontSize="33" TextColor="{Binding TitleColor}" BackgroundColor="{Static g:gigItColors.headerBG}"
                Text="{Binding IconGlyph}"
                Command="{Binding ShowMenu}">
                <ctrls:gigItIconButton.Padding>
                    <OnPlatform x:TypeArguments="Thickness">
                        <On Platform="iOS" Value="10" />
                    </OnPlatform>
                </ctrls:gigItIconButton.Padding>
            </ctrls:gigItIconButton>
            <Label Text="{Binding Title}" 
                VerticalTextAlignment="Center" HorizontalTextAlignment="Start"
                FontSize="Large" TextColor="{Binding TitleColor}"
                Grid.Column="1" Grid.Row="0">
                <Label.Margin>
                    <OnPlatform x:TypeArguments="Thickness">
                        <On Platform="iOS" Value="10" />
                    </OnPlatform>
                </Label.Margin>
            </Label>
        </Grid>
    </ContentView.Content>
</ContentView>